import React from "react";

function Card(props) {

    const info = {
        img: "insert img here",
        name: "name",
        type: "type"
    };

    function click() {
        //flip card
    }

    function flip_back() {
        //card flips back to show back
    }
}