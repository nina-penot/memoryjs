import Board from "../Board/Board";
import Card from "../Card/Card";

function Game() {
    function check_pair(allcards) {
        //if 2 cards revealed
        //check if equal
        //if true: score+1, keep cards revealed
        //if false: flip cards adter time
        //revealed cards should be blocked
    }
}

export default Game;